version: 0.0
os: linux
files:
  - source: /
    destination: /usr/app
hooks:
  ApplicationStop:
    - location: scripts/stop-compose.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: scripts/build-and-deploy.sh
      timeout: 300
      runas: root
